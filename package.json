{
  "name": "pokemon-splendor",
  "private": true,
  "version": "1.0.0",
  "description": "Pokémon Splendor static game and ECS WebSocket room server",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "test": "for f in test/*.test.js; do node \"$f\"; done"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ws": "^8.18.3"
  }
}
